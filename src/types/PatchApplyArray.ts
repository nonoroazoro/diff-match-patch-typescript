/**
 * Represents the results of the `patch_apply` method.
 *
 * Two element Array, containing the
 * new text and an array of boolean values.
 *
 * Ported by [xiaochao.k@gmail.com](https://github.com/nonoroazoro)
 */
export type PatchApplyArray = [string, boolean[]];
